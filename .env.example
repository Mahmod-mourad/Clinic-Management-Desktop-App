# Copy this file to .env and fill in your values
# cp .env.example .env

# Secret key used to generate and validate license keys
# Generate a new one with: node -e "require('crypto').randomBytes(32).toString('hex')|console.log"
HMAC_SECRET=your_secret_here
